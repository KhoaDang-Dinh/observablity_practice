# Optional and COSTING MONEY: public browser access for the lab.
apiVersion: v1
kind: Service
metadata:
  name: grafana-public
  namespace: observability
spec:
  type: LoadBalancer
  selector:
    app: grafana
  ports:
    - name: http
      port: 80
      targetPort: 3000
